/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 16.2.3 specific configurations or defaults.
  // Turbopack is the default bundler in this version.
  // React Compiler is also stable and generally enabled.
  reactStrictMode: true, // Remains enabled for development best practices.
};

export default nextConfig;
